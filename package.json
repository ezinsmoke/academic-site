{
  "name": "academic-site-eleventy",
  "version": "0.1.0",
  "private": true,
	"scripts": {
  "start": "npx @11ty/eleventy --serve",
  "build": "npx @11ty/eleventy",
  "deploy": "npm run build && gh-pages -d _site"
	},
  "dependencies": {
    "@11ty/eleventy": "^3.1.5"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
